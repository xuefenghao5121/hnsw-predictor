# C 方案原型验证结果（关键发现）

> 日期: 2026-07-24
> 状态: ⚠️ 原型验证发现根本性问题

---

## 实验设置

- 数据: SIFT 1M, dim=128
- 分区数: 200
- 每分区: ~5000 向量
- sub-HNSW: M=16, efC=200, ef=50
- meta-index: 200 质心（每分区一个）

---

## 关键结果

### BFS 割边分区
| k_routes | recall | QPS |
|----------|--------|-----|
| 1 | 2.9% | 35 |
| 2 | 4.9% | 18 |
| 3 | 7.0% | 16 |

### K-means 聚类分区
| k_routes | recall | QPS |
|----------|--------|-----|
| 1 | 4.0% | 1872 |
| 2 | 6.9% | 1129 |
| 3 | 9.7% | 823 |
| 5 | 14.1% | 530 |
| 10 | 21.7% | 279 |
| 20 | 34.2% | 145 |
| **全部 192 分区** | **89.6%** | - |

---

## 根本性问题

### 1. 分区化本身导致 recall 上限 89.6%
即使搜索**所有** 192 个分区，recall 也只有 89.6%。

**原因**: 每个分区独立构建 sub-HNSW，**丢失了全局图结构**。
- 全量 HNSW 的图边跨越多个分区
- 独立 sub-HNSW 只保留分区内边
- 跨分区的图导航路径被切断

### 2. 路由不准（次要问题）
meta-index 路由到 k 个分区时，recall 随 k 增长缓慢：
- k_routes=2: 6.9%（vs 全量的 89.6%）
- 说明真近邻分散在**很多**分区中，2-3 个分区无法覆盖

### 3. 跨分区近邻分布
top-100 近邻中：
- 平均涉及多个不同分区
- 大部分查询的 top-10 涉及跨分区访问

---

## 与 d-HNSW 的差异

| 维度 | d-HNSW | 当前实现 |
|------|--------|----------|
| 分区数 | 500 | 200 |
| **跨分区边** | ✅ 保留 | ❌ 丢失 |
| sub-HNSW 构建 | 保留全局边 | 独立重建 |
| Recall | 94% | 89.6%（全量）/ 6.9%（路由） |

**核心差异**: d-HNSW 的 sub-HNSW **保留了跨分区边**（overflow 空间 + 分区间连接），而我的实现独立重建 sub-HNSW，切断了跨分区导航。

---

## d-HNSW 如何保持 94% recall

重读论文（arXiv:2603.13591）：

1. **balanced clustering**: 用聚类分区（不是随机/BFS）
2. **RDMA-friendly layout**: 相邻分区共享 overflow 空间
3. **保留跨分区边**: 分区间连接允许搜索时跨分区导航
4. **query-aware batch loading**: 一批查询的分区需求去重

关键: **d-HNSW 不是独立 sub-HNSW，而是全局图的分区布局**。搜索时沿全局图导航，按需加载分区。

---

## 修正方案

### 方案 C':全局图 + 分区布局（正确的 d-HNSW 风格）

1. **构建全量 HNSW**（1M 节点完整图）
2. **提取全量图结构**（level 0 邻接表）
3. **按 K-means 分区**，但**保留跨分区边**
4. **分区存储**: 每个分区包含
   - 局部节点向量
   - 局部图边（分区内）
   - **跨分区出口边**（指向其他分区的节点）
5. **搜索**:
   - meta-index 路由到入口分区
   - 在全局图上搜索（BlockCache 按需加载分区）
   - 遇到跨分区边时，加载目标分区

**这实际上回到了当前 HNSW-Predictor 的 block 加载模型**，只是 block 粒度变成了分区粒度（更大）。

### 关键洞察

C 方案（独立 sub-HNSW）**行不通**，因为丢失全局图。

正确方向应该是：
- 保留当前的**全局图 + 按需加载**模型（recall 100%）
- 优化点在于**加载粒度**（block → 分区）和**预取策略**

这其实和 baseline（F2 blocking, recall 100%）是同一个模型，只是 block 更大。

---

## 建议

### 选项 1: 放弃独立分区，回到全局图优化
- baseline (F2 blocking) 已经 recall 100%, QPS 100
- 优化方向: 更大的 block（分区级）+ 查询间预取
- 风险低，recall 保证

### 选项 2: 实现 C'（全局图 + 分区布局 + 跨分区边）
- 需要提取全量图结构
- 分区级加载 + 跨分区导航
- 预期: recall 接近 100%, QPS 提升（分区级 I/O 更少）
- 工作量大

### 选项 3: 接受近似（d-HNSW 风格，recall 94%）
- 用户明确要求 recall 不能降
- ❌ 不符合要求

---

## 文件

- BFS 分区: `output/partitions_bfs200/`
- K-means 分区: `output/partitions_kmeans200/`
- 分区脚本: `scripts/partition_kmeans.py`, `scripts/build_sub_hnsw_kmeans.py`
- 原型: `scripts/prototype_partitioned.py`